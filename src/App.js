import React from 'react';
import IPJourneyTracker from './IPJourneyTracker'; 
  // We assume IPJourneyTracker.js is in the same folder as App.js

function App() {
  return (
    <div>
      <IPJourneyTracker />
    </div>
  );
}

export default App;
